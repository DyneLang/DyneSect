#include <iostream>

// dynediff: compares the original Newton ROM image against the ROM produced
// by reassembling dynesect's output, and reports any differences.

int main(int argc, char *argv[])
{
    std::cout << "dynediff: Newton ROM comparison tool\n";
    return 0;
}
